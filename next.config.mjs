/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "assets.cdn.dicoding.com",
      // add other domains here if needed
    ],
  },
  // ...other config
};

export default nextConfig;